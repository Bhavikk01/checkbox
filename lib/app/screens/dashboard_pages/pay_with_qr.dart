import 'package:flutter/material.dart';

class PayWithQrCode extends StatelessWidget {
  const PayWithQrCode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
